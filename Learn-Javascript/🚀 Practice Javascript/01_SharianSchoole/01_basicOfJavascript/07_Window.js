// * Js এর মধ্যে কিছু ফিচার আছে যেগুলো আমরা ব্যবহার করে থাকি কিন্তু সেইগুলো Js language এর মধ্যে নেই এবং সেই এই ফিচার কে Window (Box of Features given by browser)  বলা হয়ে থাকে এবং এইগুলো ব্রাউজার এর মধ্যেমে ব্যবহার করা যায়।

// * -> Var add window objects
// -> Browser → Inspect Element → Console → window এখানে var a এর ভেলু দেখাবে

var a = 12;

// * -> Doesn't adds window objects
// -> Browser → Inspect Element → Console → window এখানে let a এর ভেলু দেখাবে না
let b = 12;
